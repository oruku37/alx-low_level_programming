#include "main.h"

/**
 * swap_int - swap the values in two variables
 * @a: variable to be swapped
 * @b: variable to be swapped
 *
 */

void swap_int(int *a, int *b)
{
	int c = 0;

	c = *a
	*a = *b
	*b = c
}
